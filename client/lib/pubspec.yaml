name: etourn_client
description: "Client pvp arena."
publish_to: 'none'
version: 0.1.0
environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.35.0
  space_core:
    path: ../../core

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
